import { StyleSheet } from 'react-native';
import { defaultContainer } from 'styles/utils';

const styles = StyleSheet.create({
  container: {
    ...defaultContainer()
  },
});

export default styles;
